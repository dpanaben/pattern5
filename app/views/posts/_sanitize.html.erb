<% if @sanitizes != nil && @sanitizes.size > 0 %>
<h1>現在法典：<%=@portfolio.name%></h1>
  <h2><%=(t "sanitize.page.index.title")%></h2>
  <table class="table">
    <thead>
      <tr>
        <th><%=(t "sanitize.description")%></th>
        <th><%=(t "sanitize.match")%></th>
        <th><%=(t "sanitize.result")%></th>
      </tr>
    </thead>

    <tbody>
      <% @sanitizes.reverse.each do |sanitize| %>
        <tr>
          <td><%= sanitize.description %></td>
          <td><%= sanitize.match %></td>
          <td><%= sanitize.result %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
<h1>你還沒有選擇任何的<%=(t 'activerecord.models.portfolio')%>！<h1>
<img src="http://lorempixel.com/640/480/cats" />
<% end %>
