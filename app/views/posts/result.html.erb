<div class="container">
  <div class="row">
    <div class="column">
      <h1><%=(t "post.page.show.title")%></h1>
      <!-- <%= link_to 'BREW', brew_post_path(@post), method: :post, data: { confirm: '確認後將會依照設定好的規則一個一個重新取代成新的字元或字串' }, :class => "btn btn-primary pull-right" %> -->
      <%= link_to (t "edit"), edit_post_path(@post), :class => "btn btn-default pull-right" %>
      <%= link_to (t "back"), @post, :class => "btn btn-default pull-right" %>
      <p>
        <strong><%=(t "post.title")%>: </strong>
        <%= @post.title %>
      </p>
      <p>
        <strong><%=(t "post.content")%>: </strong>
        <%= simple_format(myhighlight_result(@post.content)) %>
      </p>
    </div>
    <div class="column">
      <%= render 'sanitize'%>
    </div>
  </div>
</div>
